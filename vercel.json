{
  "buildCommand": "cd ../.. && turbo run build --filter={apps/web}...",
  "installCommand": "pnpm install",
  "ignoreCommand": "",
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "*/5 * * * *"
    }
  ]
}
